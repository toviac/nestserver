import { WowTokenInterceptor } from './wow-token.interceptor';

describe('WowTokenInterceptor', () => {
  it('should be defined', () => {
    expect(new WowTokenInterceptor()).toBeDefined();
  });
});
